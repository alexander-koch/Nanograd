    import XCTest
    @testable import Nanograd

    final class NanogradTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(Nanograd().text, "Hello, World!")
        }
    }
